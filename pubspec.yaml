name: cis3334_job_application_tracker
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  hive: any
  hive_flutter: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.0
  hive_generator: any

flutter:
  uses-material-design: true

